apiVersion: v1
kind: Service
metadata:
  name: mcp-chatbot-backend
  namespace: mcp-chatbot
  labels:
    app: mcp-chatbot-backend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: mcp-chatbot-backend
