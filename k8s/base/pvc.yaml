apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mcp-chatbot-data
  namespace: mcp-chatbot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
